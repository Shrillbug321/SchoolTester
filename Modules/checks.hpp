#include <iostream>
#ifndef checks
#define checks
namespace check
{
	
	bool check (std::string, std::string);
	bool selecting(std::string, std::string);
	bool is_digit(char);
	bool misspell(std::string, std::string);
}

#endif
